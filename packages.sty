\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages}

\RequirePackage{hyperxmp}
\RequirePackage[bookmarks,hyperindex=true,pdfencoding=auto,psdextra]{hyperref}
\hypersetup{%
  colorlinks=true,%
  citecolor=black,%
  linkcolor=caliblue,%
  anchorcolor=caliblue,%
  urlcolor=carmine,%
  pdflicenseurl={https://creativecommons.org/licenses/by-sa/4.0/},%
  pdfcaptionwriter={Eric Berquist},%
  pdfcontactemail={erb74@pitt.edu},%
  pdfcontacturl={https://keybase.io/berquist},%
  baseurl={https://github.com/berquist/intro_to_molecular_properties},%
}

\RequirePackage{algorithm}
\RequirePackage{algpseudocode}
\RequirePackage{bm}
\RequirePackage{booktabs}
\RequirePackage{braket}
% \RequirePackage{fontspec}
\RequirePackage{graphicx}
\graphicspath{{figures/}}
\RequirePackage{longtable}
\RequirePackage{makecell}
\renewcommand{\cellalign}{cl}
\RequirePackage[version=4]{mhchem}
\RequirePackage{microtype}
\RequirePackage{multirow}
\RequirePackage{pdflscape}
\RequirePackage{soul}
\RequirePackage{tabulary}
\RequirePackage[flushleft]{threeparttable}

\RequirePackage[usenames,dvipsnames,svgnames,hyperref,table]{xcolor}
\definecolor{caliblue}{HTML}{015482}
\definecolor{carmine}{HTML}{960018}

\RequirePackage{cancel}
\newcommand\Ccancelto[3][black]{\renewcommand\CancelColor{\color{#1}}\cancelto{#2}{#3}}

\RequirePackage[%
    separate-uncertainty = true,%
    multi-part-units = single,%
    range-phrase = {\text{ to }},%
    range-units = single,%
    retain-explicit-plus = true,%
]{siunitx}
\DeclareSIUnit\au{a.u.}

\RequirePackage{tikz}
% Adapted from https://tex.stackexchange.com/a/9448
% Introduce a new counter for counting the nodes needed for circling
\newcounter{nodecount}
% Command for making a new node and naming it according to the nodecount counter
\newcommand\tabnode[1]{\addtocounter{nodecount}{1}\tikz\node(\arabic{nodecount}){#1};}

% Some options common to all the nodes and paths
\tikzstyle{every picture}+=[remember picture,baseline]
\tikzstyle{every node}+=[inner sep=0pt,anchor=base,align=center,outer sep=1.5pt]

\newcommand{\hefe}{Hellmann\textendash{}Feynman}
\newcommand{\hf}{Hartree\textendash{}Fock}
\newcommand{\ks}{Kohn\textendash{}Sham}
\newcommand{\qchem}{\textsc{Q-Chem}}
\newcommand{\schrod}{Schr{\"{o}}dinger}
\newcommand{\szabo}{Szabo \& Ostlund}

\hyphenation{electro-statics wave-function}

\renewcommand{\thefootnote}{\alph{footnote}}

\endinput
